version: '3.8'

services:
  deepwiki-scraper:
    build: .
    ports:
      - "127.0.0.1:8000:8000"  # 前端（仅本地访问）
      - "127.0.0.1:8001:8001"  # 后端 API（仅本地访问）
    volumes:
      - ./output:/app/output
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
